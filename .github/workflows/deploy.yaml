name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t asgrm/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true asgrm/react-test npm test

      - run: docker build -t asgrm/multi-client ./client
      - run: docker build -t asgrm/multi-nginx ./nginx
      - run: docker build -t asgrm/multi-server ./server
      - run: docker build -t asgrm/multi-worker ./worker

      - run: docker push asgrm/multi-client
      - run: docker push asgrm/multi-nginx
      - run: docker push asgrm/multi-server
      - run: docker push asgrm/multi-worker
